name: Frontend CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Node.js (FIXED for Vite)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3. Move into correct folder (IMPORTANT FIX)
      - name: Install dependencies
        run: |
          cd Online_Shopping_Full_Stack
          npm install

      # 4. Build project
      - name: Build
        run: |
          cd Online_Shopping_Full_Stack
          npm run build

      # 5. Safe test step
      - name: Test
        run: echo "No tests configured"
